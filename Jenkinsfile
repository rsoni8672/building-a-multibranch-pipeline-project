pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                echo 'node version is'
                sh 'node -v'
                sh 'npm install' 
            }
        }
    }
}
